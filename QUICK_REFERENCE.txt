╔══════════════════════════════════════════════════════════════╗
║          BILLETERIE DEPLOYMENT QUICK REFERENCE              ║
╚══════════════════════════════════════════════════════════════╝

🎯 RASPBERRY PI: 192.168.1.24
🌐 DOMAIN: les-ecoworkers.fr
📦 DEPLOY PATH: /home/pi/apps/billeterie

┌──────────────────────────────────────────────────────────────┐
│ FIRST TIME SETUP                                             │
└──────────────────────────────────────────────────────────────┘

1️⃣ On Raspberry Pi:
   scp setup_raspberry_pi.sh pi@192.168.1.24:~
   ssh pi@192.168.1.24
   bash setup_raspberry_pi.sh

2️⃣ On Windows:
   git add .
   git commit -m "Configure deployment"
   git push origin master
   bundle exec cap production deploy:check
   bundle exec cap production deploy
   bundle exec cap production puma:systemd:config puma:systemd:enable

3️⃣ Start Services (on Pi):
   sudo systemctl start puma_billeterie_production.service
   sudo systemctl restart apache2

┌──────────────────────────────────────────────────────────────┐
│ REGULAR DEPLOYMENT (After code changes)                     │
└──────────────────────────────────────────────────────────────┘

git add .
git commit -m "Your message"
git push origin master
bundle exec cap production deploy

┌──────────────────────────────────────────────────────────────┐
│ USEFUL COMMANDS                                              │
└──────────────────────────────────────────────────────────────┘

# Check before deploy
bundle exec cap production deploy:check

# Deploy
bundle exec cap production deploy

# Restart
bundle exec cap production puma:restart

# Rollback
bundle exec cap production deploy:rollback

# View logs
bundle exec cap production puma:log

# SSH to Pi
ssh pi@192.168.1.24

┌──────────────────────────────────────────────────────────────┐
│ IMPORTANT FILES ON RASPBERRY PI                              │
└──────────────────────────────────────────────────────────────┘

📄 /home/pi/apps/billeterie/shared/config/database.yml
📄 /home/pi/apps/billeterie/shared/config/master.key
📄 /home/pi/apps/billeterie/shared/.env

┌──────────────────────────────────────────────────────────────┐
│ TROUBLESHOOTING                                              │
└──────────────────────────────────────────────────────────────┘

❌ Deployment fails:
   → bundle exec cap production deploy:check

❌ Can't SSH:
   → ssh pi@192.168.1.24
   → Check: Is Pi on? Is 192.168.1.24 correct IP?

❌ Site doesn't load:
   → ssh pi@192.168.1.24
   → sudo systemctl status puma_billeterie_production.service
   → sudo systemctl status apache2
   → sudo tail -f /var/log/apache2/billeterie_error.log

❌ Database issues:
   → Check /home/pi/apps/billeterie/shared/config/database.yml
   → sudo systemctl status postgresql

┌──────────────────────────────────────────────────────────────┐
│ MONITORING LOGS                                              │
└──────────────────────────────────────────────────────────────┘

# Application logs
tail -f /home/pi/apps/billeterie/current/log/production.log

# Puma logs
tail -f /home/pi/apps/billeterie/shared/log/puma.error.log

# Apache logs
sudo tail -f /var/log/apache2/billeterie_error.log

┌──────────────────────────────────────────────────────────────┐
│ SSL SETUP (After basic deployment works)                    │
└──────────────────────────────────────────────────────────────┘

# On Raspberry Pi
sudo apt-get install certbot python3-certbot-apache
sudo certbot --apache -d les-ecoworkers.fr -d www.les-ecoworkers.fr

# Then update config/environments/production.rb
# Change: config.force_ssl = false → config.force_ssl = true

┌──────────────────────────────────────────────────────────────┐
│ NETWORK CONFIGURATION                                        │
└──────────────────────────────────────────────────────────────┘

🔧 Router Port Forwarding:
   External Port 80 → 192.168.1.24:80 (HTTP)
   External Port 443 → 192.168.1.24:443 (HTTPS)

🌐 DNS Configuration:
   les-ecoworkers.fr → Your Public IP
   www.les-ecoworkers.fr → Your Public IP

┌──────────────────────────────────────────────────────────────┐
│ NEED MORE HELP?                                              │
└──────────────────────────────────────────────────────────────┘

📖 Complete Guide: DEPLOYMENT.md
✅ Checklist: DEPLOY_CHECKLIST.md
🚀 Full README: DEPLOYMENT_README.md

╔══════════════════════════════════════════════════════════════╗
║  Ready to deploy! Start with: .\check_deployment.ps1       ║
╚══════════════════════════════════════════════════════════════╝
