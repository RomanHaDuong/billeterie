<div class="container p-1 p-sm-3 p-md-5 rounded-3" style="background-color: #E9D7C3;">
  
  <h1 class="text-center text-primary mb-3 mb-md-5">
    Programme
  </h1>

  <%# Explanatory text %>
  <div class="alert alert-info mb-4" style="background-color: #F5E6D3; border: 2px solid #6B7459; border-radius: 10px;">
    <h5 class="fw-bold mb-3" style="color: #595E47;">
      <i class="fa-solid fa-circle-info me-2"></i>Pour entrer dans le mouvement, dites-nous que vous venez au Festival !
    </h5>
    <p class="mb-3" style="color: #595E47;">
      C'est un mouvement en deux temps :
    </p>
    <ul class="mb-3" style="color: #595E47;">
      <li class="mb-2">Que vous veniez pour un seul atelier ou pour tout le temps du Festival, <strong>inscrivez-vous en cliquant sur le bouton "Je viens!"</strong> : l'inscription, en participation consciente, est nécessaire pour comprendre l'expérience proposée et recevoir toutes les infos</li>
      <li>Inscrivez-vous pour chaque atelier qui vous intéresse ! Et n'oubliez pas d'annuler si vous ne pouvez plus vous joindre à nous</li>
    </ul>
    <div class="text-center">
      <%= link_to "https://opencollective.com/le-47/events/lheure-du-mouvement-111636a3/order/97285?interval=oneTime&amount=10&name=&legalName=&email=", target: "_blank", class: "btn btn-lg fw-bold", style: "background-color: #6B7459; color: white; border: none; padding: 12px 30px;" do %>
        <i class="fa-solid fa-heart me-2"></i>Je viens !
      <% end %>
    </div>
  </div>

  <%# Plant decoration wrapper - starts just above the buttons %>
  <div class="position-relative">
    <%# Left plants - repeating pattern - positioned at screen edge %>
    <div class="position-absolute d-flex flex-column" style="left: calc(-50vw + 50%); top: -120px; bottom: -200px; width: 200px; pointer-events: none; z-index: 0; opacity: 0.5;">
      <%= image_tag "plant_top_end_left.png", style: "width: 200px; display: block; flex-shrink: 0;", alt: "" %>
      <div style="background-image: url('<%= asset_path('left_plants.png') %>'); background-repeat: repeat-y; background-position: right top; background-size: 200px auto; flex: 1;"></div>
    </div>
    
    <%# Right plants - repeating pattern - positioned at screen edge %>
    <div class="position-absolute d-flex flex-column" style="right: calc(-50vw + 50%); top: -120px; bottom: -200px; width: 200px; pointer-events: none; z-index: 0; opacity: 0.5;">
      <%= image_tag "plant_top_end_right.png", style: "width: 200px; display: block; flex-shrink: 0;", alt: "" %>
      <div style="background-image: url('<%= asset_path('right_plants.png') %>'); background-repeat: repeat-y; background-position: left top; background-size: 200px auto; flex: 1;"></div>
    </div>

  <div class="d-flex justify-content-center align-items-center mb-4">
    <ul class="nav nav-pills">
      <li class="nav-item">
        <a id="listeTab" class="nav-link" data-bs-toggle="tab" href="#list-view" style="background-color: transparent; color: #595E47; border: 2px solid #6B7459;">Liste</a>
      </li>
      <li class="nav-item">
        <a id="agendaTab" class="nav-link active" data-bs-toggle="tab" href="#agenda-view" style="background-color: #6B7459; color: white; border: 2px solid #6B7459;">Agenda</a>
      </li>
    </ul>
  </div>

    <div class="container py-2 py-md-5">

      <div class="tab-content">

        <%# list view %>
        <div class="tab-pane fade" id="list-view">
          <div class="container-fluid">
            <div class="row g-4">
              <% @offres.each do |offre| %>

                <div class="col-sm-6 col-md-4 mb-4">

                  <div class="card workshop-card h-100 shadow-sm">
                    <%# Image Section %>
                    <div class="workshop-card-img-wrapper">
                      <% if offre.image.attached? %>
                        <%= image_tag offre.image, class: "workshop-card-img" %>
                      <% else %>
                      <%= image_tag "placeholder_offre.png", class: "workshop-card-img" %>
                    <% end %>
                  </div>

                  <div class="card-body position-relative">
                    <%# Content %>
                    <h5 class="card-title"><%= offre.titre %></h5>

                    <p class="card-text" style="-webkit-line-clamp: 3; -webkit-box-orient: vertical; overflow: hidden; display: -webkit-box;">
                      <%= offre.sous_titre %>
                    </p>

                    <%# Seat Availability %>
                    <div class="mb-2">
                      <% if offre.full? %>
                        <span class="badge bg-danger">
                          <i class="fa-solid fa-users me-1"></i>
                          Complet
                        </span>
                      <% else %>
                        <span class="badge" style="background-color: #6B7459; color: white;">
                          <i class="fa-solid fa-users me-1"></i>
                          <%= offre.bookings.count %>/<%= offre.place %> places
                        </span>
                      <% end %>
                    </div>

                    <%# Registration Status %>
                    <div class="mb-3">
                      <% if current_user && offre.user_registered?(current_user) %>
                        <div class="mt-2">
                          <span class="badge bg-success">
                            <i class="fa-solid fa-check me-1"></i>
                            Inscrit
                          </span>
                        </div>
                      <% end %>
                    </div>

                    <%# Footer %>
                    <div class="mt-4 d-flex justify-content-between align-items-center">
                      <div class="col-auto">
                        <div class="date-badge text-center">
                          <div class="month text-uppercase small text-nowrap">
                            <%= offre.date_prevue.strftime("%d/%m") %>
                          </div>
                          <div class="day fw-bold fs-6 text-nowrap">
                            <%= format_time_french(offre.date_prevue) %>
                          </div>
                        </div>
                      </div>
                      <%= link_to offre_path(offre), class: 'btn btn-outline-primary btn-sm' do %>
                        Voir plus <i class="fa-solid fa-arrow-right ms-2"></i>
                      <% end %>
                    </div>
                  </div>
                </div>
              </div>

            <% end %>
          </div>
        </div>
      </div>

      <%# agenda view %>
      <div class="tab-pane fade show active" id="agenda-view">
        <%= render 'shared/offres_agenda_view' %>
      </div>

    </div>

  </div>
  </div>
  <%# End plant decoration wrapper %>
</div>

<script>
  function initOffresIndex() {
    const listeTab = document.getElementById('listeTab');
    const agendaTab = document.getElementById('agendaTab');
    
    listeTab.addEventListener('click', function(e) {
      e.preventDefault();
      // Update styles
      listeTab.style.setProperty('background-color', '#6B7459', 'important');
      listeTab.style.setProperty('color', 'white', 'important');
      agendaTab.style.setProperty('background-color', 'transparent', 'important');
      agendaTab.style.setProperty('color', '#595E47', 'important');
      
      // Trigger Bootstrap tab
      const tab = new bootstrap.Tab(listeTab);
      tab.show();
    });
    
    agendaTab.addEventListener('click', function(e) {
      e.preventDefault();
      // Update styles
      agendaTab.style.setProperty('background-color', '#6B7459', 'important');
      agendaTab.style.setProperty('color', 'white', 'important');
      listeTab.style.setProperty('background-color', 'transparent', 'important');
      listeTab.style.setProperty('color', '#595E47', 'important');
      
      // Trigger Bootstrap tab
      const tab = new bootstrap.Tab(agendaTab);
      tab.show();
    });
  }

  // Initialize on both regular page load and Turbo navigation
  document.addEventListener('DOMContentLoaded', initOffresIndex);
  document.addEventListener('turbo:load', initOffresIndex);
</script>
