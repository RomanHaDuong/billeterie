  <div class="container-fluid">
    <div class="row g-4">
      <% @offres.each do |offre| %>
        <%= render partial: 'shared/offre', locals: { offre: offre } %>
      <% end %>
    </div>
  </div>
