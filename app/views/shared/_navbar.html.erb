<nav class="navbar navbar-expand-lg navbar-light bg-white py-3 shadow-sm">
  <div class="container">
    <%= link_to root_path, class: "navbar-brand d-flex align-items-center" do %>
      <%= image_tag "logo.png", class: "rounded-circle me-2", width: 40, height: 40, alt: "Festival logo" %>
      <span class="fw-bold fs-4 text-primary">Festival</span>
    <% end %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent" aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarContent">
      <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to 'Programme', offres_path, class: 'nav-link px-3 fw-medium' %>
        </li>
        <li class="nav-item">
          <%= link_to 'Intervenants', fournisseurs_path, class: 'nav-link px-3 fw-medium' %>
        </li>
        <li class="nav-item">
          <%= link_to 'Le 47', lieu_path, class: 'nav-link px-3 fw-medium' %>
        </li>
        <% if current_user %>
          <li class="nav-item">
            <%# <%= link_to "Mes réservations", bookings_path, class: 'nav-link px-3 fw-medium' %>
          </li>
          <li class="nav-item">
            <%# <%= link_to "Mes favoris", favoris_path, class: 'nav-link px-3 fw-medium' %>
          </li>
        <% end %>
      </ul>

      <div class="d-flex align-items-center gap-3">
        <% if user_signed_in? %>
          <%= link_to user_path(current_user), class: 'nav-link' do %>
            <i class="bi bi-person-circle me-1"></i>
            <span>Tableau de bord</span>
          <% end %>

          <% if current_user.image.attached? %>
            <%= image_tag current_user.image,
                class: 'rounded-circle me-2',
                size: '32x32',
                alt: 'Profile picture' %>
          <% else %>
            <%= image_tag "placeholder_person.png",
                class: 'rounded-circle me-2',
                size: '32x32',
                alt: 'Placeholder picture' %>
          <% end %>
        <% else %>
          <%= link_to 'Connexion', new_user_session_path, class: 'btn btn-outline-primary me-2' %>
          <%= link_to "S'inscrire", new_user_registration_path, class: 'btn btn-primary' %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
