<nav class="navbar navbar-expand-lg navbar-light bg-white py-3 shadow-sm">
  <div class="container">
    <%= link_to root_path, class: "navbar-brand d-flex align-items-center" do %>
      <%= image_tag "logo.png", class: "rounded-circle me-2", width: 40, height: 40, alt: "Festival logo" %>
      <span class="fw-bold fs-4 text-primary">Festival</span>
    <% end %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent" aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarContent">
      <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to 'Le Programme', offres_path, class: 'nav-link px-3 fw-medium', style: 'color: #595E47;' %>
        </li>
        <li class="nav-item">
          <%= link_to 'Animateurs', fournisseurs_path, class: 'nav-link px-3 fw-medium', style: 'color: #595E47;' %>
        </li>
        <li class="nav-item">
          <%= link_to 'Le 47', lieu_path, class: 'nav-link px-3 fw-medium', style: 'color: #595E47;' %>
        </li>
      </ul>

      <div class="d-flex flex-column flex-lg-row align-items-center gap-3">
      <div class="d-flex flex-column flex-md-row align-items-center gap-2 mb-2 mb-lg-0">
        <%= link_to "Je viens", participation_path, class: 'btn', style: 'background-color: #7B8467; color: white; border: none;' %>
        <small class="text-center text-md-start">
          <%= link_to "Je ne peux pas venir mais je soutiens!", "https://opencollective.com/le-47/contribute/contribution-financiere-77696/checkout?interval=month&amount=50&contributeAs=me", class: 'text-decoration-none', style: 'font-size: 0.75rem; color: #595E47;', target: :blank %>
        </small>
      </div>

        <% if user_signed_in? %>
          <div class="d-flex align-items-center gap-2">
            <%= link_to dashboard_path, class: 'btn btn-primary' do %>
              <i class="bi bi-person-circle me-2"></i>
              <span>Mon compte festivalier</span>
            <% end %>

            <% if current_user.image.attached? %>
              <%= image_tag current_user.image,
                  class: 'rounded-circle',
                  size: '32x32',
                  alt: 'Profile picture' %>
            <% else %>
              <%= image_tag "placeholder_person.png",
                  class: 'rounded-circle',
                  size: '32x32',
                  alt: 'Placeholder picture' %>
            <% end %>

            <% if current_user.admin? %>
              <button id="cms-edit-toggle" class="btn <%= session[:edit_mode] ? 'btn-warning active' : 'btn-outline-secondary' %> btn-sm rounded-circle" title="Toggle edit mode" type="button" style="width: 38px; height: 38px; padding: 0; display: flex; align-items: center; justify-content: center;">
                <i class="fa fa-pen"></i>
              </button>
            <% end %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</nav>
