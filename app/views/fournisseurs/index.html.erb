<div class="container p-1 p-sm-3 p-md-5 my-5 bg-light rounded-3">
  <h1 class="text-center mb-5" style="color: #193535;">
    Animateurs
  </h1>

  <div class="container-fluid">
    <div class="row g-4">
      <% @fournisseurs.each do |fournisseur| %>
        <%= render partial: 'shared/person_card', locals: { fournisseur: fournisseur } %>
      <% end %>
    </div>
  </div>
</div>
